; 새로운 연산을 집어넣을때
; * 직접 나눤 맡기는 방식
;   - 새로운 연산에 대한 일반화된 프로시저 작성
; * 데이터 중심 방식
;   - 일반화된 연산을 추가한다.
;   - (put '연산 '타입) 을 타입 갯수만큼 수행한다.
; * 메시지 패싱 방식
;   - 모든 타입에 대해서 dispatch 프로시저를 생성하는 프로시저를 수정한다.

; 새로운 타입을 보탤때
; * 직접 나눤 맡기는 방식
;   - 새로운 생성자 추가
;   - 기존의 일반화된 프로시저에 새로운 타입 관련 dispatch 코드 추가
; * 데이터 중심 방식
;   - (put '연산 '타입) 을 타입 갯수만큼 수행한다.
; * 메시지 패싱 방식
;   - dispatch 프로시저를 생성하는 프로시저를 추가한다.

; 새로운 데이터 타입을 집어넣어야 할 일이 많다면
; - 메시지 패싱 방식

; 새로운 연산을 덧붙이는 경우가 많을 때
; - 데이터 중심 방식
